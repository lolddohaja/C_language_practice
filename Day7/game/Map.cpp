#include <vector>
#include <cstring>
#include <string>

using namespace std;

class Map
{
private:
    vector<string> map;
    int width;
    int height;

public:
    void load_map(class clas, char *mapName)
    {
        
    }
    void print_map(class clas, int cnt);
    void start_game(class clas);
};

